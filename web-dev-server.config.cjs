module.exports = {
  port: 3000,
  nodeResolve: true, // para que pueda resolver los modulos de node
  open: true, // para que abra el navegador
  watch: true, // para que este escuchando los cambios
  appIndex: 'dist/index.html', // para que sepa donde se encuentra el archivo principal
}